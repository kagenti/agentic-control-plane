apiVersion: v1
kind: Service
metadata:
  name: a2a-bridge
  namespace: kagenti-agents
  labels:
    app: a2a-bridge
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8000
      protocol: TCP
      targetPort: 8000
  selector:
    app: a2a-bridge
