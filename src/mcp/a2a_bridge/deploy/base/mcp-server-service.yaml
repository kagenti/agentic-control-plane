apiVersion: v1
kind: Service
metadata:
  name: a2a-mcp-bridge
  namespace: a2a-mcp-bridge
  labels:
    app: a2a-mcp-bridge
spec:
  selector:
    app: a2a-mcp-bridge
  ports:
  - name: http
    port: 8000
    targetPort: 8000
    protocol: TCP
  type: ClusterIP
